name: Dependabot manager

on:
  workflow_dispatch:

jobs:
  update_dependencies:
    runs-on: ubuntu-latest
    steps:
      - name: Run Dependabot updates
        uses: aribanerjee/Dependabot-manager@v1.0.8
        with:
          package-ecosystem: 'npm'
