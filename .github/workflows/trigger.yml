name: Dependabot updates

on:
  workflow_dispatch:

jobs:
  update_dependencies:
    runs-on: ubuntu-latest
    steps:
      - name: Run Dependabot updates
        uses: aribanerjee/Dependabot-manager@v1.0.1
        with:
          package-ecosystem: 'npm'
